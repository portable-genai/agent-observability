"""Adapter families: ``gcp`` (managed Cloud Logging WORM), ``local`` (SDK-free SQLite WORM
stand-in, the dev / test default) and ``onprem`` (fail-fast migration placeholders)."""

from __future__ import annotations
